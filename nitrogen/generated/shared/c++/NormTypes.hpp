///
/// NormTypes.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include <cmath>
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroopencv {

  /**
   * An enum which can be represented as a JavaScript enum (NormTypes).
   */
  enum class NormTypes {
    NORM_INF      SWIFT_NAME(normInf) = 1,
    NORM_L1      SWIFT_NAME(normL1) = 2,
    NORM_L2      SWIFT_NAME(normL2) = 4,
    NORM_L2SQR      SWIFT_NAME(normL2sqr) = 5,
    NORM_HAMMING      SWIFT_NAME(normHamming) = 6,
    NORM_HAMMING2      SWIFT_NAME(normHamming2) = 7,
    NORM_TYPE_MASK      SWIFT_NAME(normTypeMask) = 7,
    NORM_RELATIVE      SWIFT_NAME(normRelative) = 8,
    NORM_MINMAX      SWIFT_NAME(normMinmax) = 32,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroopencv

namespace margelo::nitro {

  using namespace margelo::nitro::nitroopencv;

  // C++ NormTypes <> JS NormTypes (enum)
  template <>
  struct JSIConverter<NormTypes> final {
    static inline NormTypes fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<NormTypes>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, NormTypes arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double integer;
      double fraction = modf(value.getNumber(), &integer);
      if (fraction != 0.0) {
        // It is some kind of floating point number - our enums are ints.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 8;
    }
  };

} // namespace margelo::nitro
