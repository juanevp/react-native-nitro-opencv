///
/// MatImageFormat.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroopencv {

  /**
   * An enum which can be represented as a JavaScript union (MatImageFormat).
   */
  enum class MatImageFormat {
    JPEG      SWIFT_NAME(jpeg) = 0,
    PNG      SWIFT_NAME(png) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroopencv

namespace margelo::nitro {

  using namespace margelo::nitro::nitroopencv;

  // C++ MatImageFormat <> JS MatImageFormat (union)
  template <>
  struct JSIConverter<MatImageFormat> final {
    static inline MatImageFormat fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("jpeg"): return MatImageFormat::JPEG;
        case hashString("png"): return MatImageFormat::PNG;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum MatImageFormat - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, MatImageFormat arg) {
      switch (arg) {
        case MatImageFormat::JPEG: return JSIConverter<std::string>::toJSI(runtime, "jpeg");
        case MatImageFormat::PNG: return JSIConverter<std::string>::toJSI(runtime, "png");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert MatImageFormat to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("jpeg"):
        case hashString("png"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
