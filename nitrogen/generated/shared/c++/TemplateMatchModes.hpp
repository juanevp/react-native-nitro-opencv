///
/// TemplateMatchModes.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include <cmath>
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroopencv {

  /**
   * An enum which can be represented as a JavaScript enum (TemplateMatchModes).
   */
  enum class TemplateMatchModes {
    TM_SQDIFF      SWIFT_NAME(tmSqdiff) = 0,
    TM_SQDIFF_NORMED      SWIFT_NAME(tmSqdiffNormed) = 1,
    TM_CCORR      SWIFT_NAME(tmCcorr) = 2,
    TM_CCORR_NORMED      SWIFT_NAME(tmCcorrNormed) = 3,
    TM_CCOEFF      SWIFT_NAME(tmCcoeff) = 4,
    TM_CCOEFF_NORMED      SWIFT_NAME(tmCcoeffNormed) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroopencv

namespace margelo::nitro {

  using namespace margelo::nitro::nitroopencv;

  // C++ TemplateMatchModes <> JS TemplateMatchModes (enum)
  template <>
  struct JSIConverter<TemplateMatchModes> final {
    static inline TemplateMatchModes fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<TemplateMatchModes>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, TemplateMatchModes arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double integer;
      double fraction = modf(value.getNumber(), &integer);
      if (fraction != 0.0) {
        // It is some kind of floating point number - our enums are ints.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 5;
    }
  };

} // namespace margelo::nitro
